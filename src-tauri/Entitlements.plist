<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened Runtime is REQUIRED for notarization (macOS 10.14.5+) -->

    <!-- Allow JIT compilation (needed by WKWebView / JavaScriptCore) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (needed by WKWebView) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation (needed for WebView plugins / dylibs) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network client access (updater, AI assistant API calls) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Read-write access to user-selected files (file dialogs) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Read-only access to Downloads folder (evidence scanning) -->
    <key>com.apple.security.files.downloads.read-only</key>
    <true/>
</dict>
</plist>
